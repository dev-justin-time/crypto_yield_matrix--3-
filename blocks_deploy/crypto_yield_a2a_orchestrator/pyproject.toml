[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto_yield_a2a_orchestrator"
version = "1.0.0"
description = "Crypto Yield Matrix A2A orchestrator"
requires-python = ">=3.12"
dependencies = [
    "blocks-network",
]

[tool.setuptools]
py-modules = ["handler"]
