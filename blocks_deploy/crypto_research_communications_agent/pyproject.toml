[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crypto_research_communications_agent"
version = "1.0.0"
description = "crypto_research_communications_agent agent"
requires-python = ">=3.12"
dependencies = [
    "blocks-network",
]

[tool.setuptools]
py-modules = ["handler"]

packages = ["blocks_agents", "blocks_agents.handlers"]
