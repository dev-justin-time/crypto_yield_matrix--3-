[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tokenomics_sustainability_expert"
version = "1.0.0"
description = "tokenomics_sustainability_expert agent"
requires-python = ">=3.12"
dependencies = [
    "blocks-network==1.0.11",
]

[tool.setuptools]
py-modules = ["handler"]

packages = ["blocks_agents", "blocks_agents.handlers"]
