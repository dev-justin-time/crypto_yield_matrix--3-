# Crypto Yield Matrix — File Dependency Family Tree

> Each entry shows `filename` and its dependencies (what it imports/reads) and dependents (what imports/reads it).
> Arrows: `→ reads/imports` | `← imported by` | `⇄ mutual dependency` | `⇢ mirrors/syncs to`

---

## 1. CORE DATA LAYER (no code, read by everything)

```
data/
├── yield_data.csv ............................. ← 118 canonical rows, 61 columns
│   ← common.py (load_source)  ← collector.py (load_symbols)
│   ← build.py (read_csv)  ← audit.py (read_rows)  ← sync.py (SOURCE_FILES)
│   ⇢ blocks_deploy/{11 projects}/yield_data.csv
│
├── asset_catalog.csv .......................... ← generated by build.py
│   ← common.py (load_asset_catalog)
│   ← audit.py (read_rows)
│   ⇢ blocks_deploy/{11 projects}/asset_catalog.csv
│
├── DATA_DICTIONARY.md ........................ ← read by audit.py
│   ⇢ blocks_deploy/{11 projects}/DATA_DICTIONARY.md
│
└── .generated-by-build-asset-catalog ......... ownership marker

csv/
├── source_snapshots/{59 assets}.csv .......... ← one-row market snapshots
│   ← common.py (load_asset_snapshot, snapshot_research)
│   ← build.py (source_quotes)  ← organize.py
│   ⇢ blocks_deploy/{11 projects}/csv/source_snapshots/
│
├── reference/
│   ├── contract_addresses.csv ................ 59 assets × contract metadata
│   ├── asset_summary.csv
│   ├── data_quality_notes.csv
│   ├── feature_groups.csv
│   ├── file_purposes.csv
│   ├── package_contents.csv
│   └── source_coverage.csv
│       ⇢ blocks_deploy/{11 projects}/csv/reference/
│
├── assets/{59 symbols}.csv ................... ← generated by build.py
│   ← audit.py  ⇢ blocks_deploy/{11 projects}/csv/assets/
│
├── quotes/{59 symbols}.csv ................... ← generated by build.py
│   ← audit.py  ⇢ blocks_deploy/{11 projects}/csv/quotes/
│
└── source_manifest.json ...................... ← managed by organize.py
    ← sync.py (SOURCE_FILES)  ⇢ blocks_deploy/{11 projects}/

live_data/
├── live_snapshot.json ........................ ← written by collector.py
│   ← common.py (load_live_snapshot, live_enrichment)
│   ← worker.py (read/write)  ← readiness.py (evaluate)
│   ⇢ blocks_deploy/{11 projects}/live_data/
│
├── worker_status.json ........................ ← written by worker.py
│   ← readiness.py (evaluate_status)
│
├── provider_canary_evidence.json ............. ← written by canary.py
├── distribution_contract.json ................ ← policy doc
├── live_snapshot.example.json ................ ← test fixture
├── worker_status.example.json ................ ← test fixture
└── README.md
```

---

## 2. SHARED HANDLER INFRASTRUCTURE

```
blocks_agents/
├── handlers/
│   └── common.py .............................. ★ THE HUB — imported by ALL 11 handlers
│       │  → reads    data/yield_data.csv (load_source)
│       │  → reads    data/asset_catalog.csv (load_asset_catalog)
│       │  → reads    csv/source_snapshots/*.csv (load_asset_snapshot)
│       │  → reads    live_data/live_snapshot.json (load_live_snapshot)
│       │  → exposes  task_payload, select, load_source, report, value,
│       │             derived_features, evidence, asset_enrichment,
│       │             snapshot_research, live_enrichment, numeric_value,
│       │             safe_divide, display_number, safe_subtract, status,
│       │             load_asset_snapshot, load_csv, read_context,
│       │             validate_context_files, catalog_by_symbol
│       │
│       │  ← crypto_risk_analyst.py       (asset_enrichment, derived_features, evidence,
│       │                                   load_source, report, select, snapshot_research,
│       │                                   status, task_payload, value)
│       │  ← defi_liquidity_analyst.py    (evidence, load_source, report, select, status,
│       │                                   task_payload, value)
│       │  ← yield_methodology_expert.py  (evidence, load_source, report, select, status,
│       │                                   task_payload, value)
│       │  ← data_provenance_auditor.py   (evidence, load_source, read_context, report,
│       │                                   select, status, task_payload)
│       │  ← tokenomics_sustainability_expert.py  (asset_enrichment, evidence, load_source,
│       │                                   numeric_value, report, select, status,
│       │                                   task_payload, value)
│       │  ← feature_engineering_expert.py (derived_features, evidence, load_source,
│       │                                   numeric_value, report, select, status,
│       │                                   task_payload)
│       │  ← matrix_research_insights_agent.py (evidence, load_source, report, select,
│       │                                   status, task_payload, value)
│       │  ← model_validation_guardian.py  (evidence, load_source, numeric_value, report,
│       │                                   select, status, task_payload)
│       │  ← portfolio_scenario_expert.py (evidence, load_source, numeric_value, report,
│       │                                   select, status, task_payload, value)
│       │  ← quant_forecasting_expert.py  (evidence, load_source, numeric_value, report,
│       │                                   select, status, task_payload, value)
│       │  ← crypto_research_communications_agent.py (evidence, load_source, report,
│       │                                   select, status, task_payload, value)
│       │  ← tests/test_asset_sources.py  (load_asset_snapshot, snapshot_research, common)
│       │  ⇢ blocks_deploy/{11 projects}/handlers/common.py  (mirrored by sync.py)
│       │
│       │  Key design: ROOT = Path(__file__).resolve().parents[2] = project root
│       │  _PATH_MAP resolves logical filenames → reorganized paths
│       │    (yield_data.csv, asset_catalog.csv, DATA_DICTIONARY.md, validate.md,
│       │     index.html, matrix.js, styles.css, live_data/live_snapshot.json)
│       │  CONTEXT_ALLOWLIST controls what context files handlers can read
│       │  USER_VALUE_GUIDANCE provides per-agent usage guardrails
│       │  DERIVED_CATALOG_FIELDS = (yield_momentum, mcap_to_tvl, risk_score, yield_premium)
│       │  datetime/timezone imported at module level (hoisted from 2 inner functions)
│       │  _observation_is_fresh / _snapshot_is_fresh — no local imports
│       │
│       └── report() → produces {"artifacts": [{"data": json, "mimeType": "application/json"}]}
│
├── loader.py ................................. ← imports handler modules dynamically
│   │  → reads  agent card JSON files (via ROOT / filename)
│   │  → uses   importlib.import_module() for handler callables
│   │  ← __main__.py
│   │  ⇢ blocks_deploy/{11 projects}/loader.py
│   │
├── __init__.py ............................... ← package marker
│   ⇢ blocks_deploy/{11 projects}/__init__.py
│
├── agent_cards.json .......................... ← aggregate registry
│   ⇢ blocks_deploy/{11 projects}/
│
├── {11 agent}.json ........................... ← per-agent card definitions
│   ⇢ blocks_deploy/{11 projects}/agent-card.json  (deployment-specific)
│
└── README.md ................................. ← documentation
    ⇢ blocks_deploy/{11 projects}/README.md
```

---

## 3. AGENT HANDLERS (11 files, all depend on common.py)

```
blocks_agents/handlers/
│
├── crypto_risk_analyst.py ────────────────── ★ MOST IMPORTANT HANDLER
│   → common.py: asset_enrichment, derived_features, evidence,
│                load_source, report, select, snapshot_research,
│                status, task_payload, value
│   Produces: yield + volatility + drawdown + Sharpe + beta context
│
├── defi_liquidity_analyst.py
│   → common.py: evidence, load_source, report, select, status,
│                task_payload, value
│   Produces: volume, TVL, activity, liquidity proxy analysis
│
├── yield_methodology_expert.py
│   → common.py: evidence, load_source, report, select, status,
│                task_payload, value
│   Produces: APY/APR methodology comparison and annualization flags
│
├── data_provenance_auditor.py
│   → common.py: evidence, load_source, read_context, report,
│                select, status, task_payload
│   Produces: schema audit, source conflict detection, lineage trace
│
├── tokenomics_sustainability_expert.py
│   → common.py: asset_enrichment, evidence, load_source,
│                numeric_value, report, select, status,
│                task_payload, value
│   Produces: nominal yield vs inflation/dilution/supply analysis
│
├── feature_engineering_expert.py
│   → common.py: derived_features, evidence, load_source,
│                numeric_value, report, select, status, task_payload
│   Produces: 4 transparent derived features with formula audit trail
│
├── matrix_research_insights_agent.py
│   → common.py: evidence, load_source, report, select, status,
│                task_payload, value
│   Produces: dashboard matrix → traceable research insights
│
├── model_validation_guardian.py
│   → common.py: evidence, load_source, numeric_value, report,
│                select, status, task_payload
│   Produces: leakage/circularity/split validation with FAIL gate
│
├── portfolio_scenario_expert.py
│   → common.py: evidence, load_source, numeric_value, report,
│                select, status, task_payload, value
│   Produces: what-if constraint screening with trade-off flags
│
├── quant_forecasting_expert.py
│   → common.py: evidence, load_source, numeric_value, report,
│                select, status, task_payload, value
│   Produces: cautious forecasts (currently FAIL — insufficient history)
│
├── crypto_research_communications_agent.py
│   → common.py: evidence, load_source, report, select, status,
│                task_payload, value
│   Produces: evidence-linked research notes
│
└── __main__.py ............................... ← smoke-test entry point
    → loader.py (load_card, run_card)
    → reads  blocks_agents/{agent}.json for each card
```

---

## 4. LIVE DATA PIPELINE

```
src/live/
│
├── collector.py ............................... ★ LIVE DATA ENGINE
│   │  → reads    data/yield_data.csv (load_symbols)
│   │  → uses     urllib (urlopen, Request)
│   │  → calls    Binance API  (api.binance.com)
│   │  → calls    Coinbase API (api.exchange.coinbase.com)
│   │  → calls    DeFiLlama    (api.llama.fi)
│   │  → calls    Ethereum RPC (cloudflare-eth.com)
│   │  → calls    Solana RPC   (api.mainnet-beta.solana.com)
│   │  → writes   live_data/live_snapshot.json
│   │  → writes   live_data/worker_status.json
│   │  → exposes  LiveDataCollector, ProviderClient, ProviderState,
│   │             load_symbols, write_snapshot, is_fresh, validate_provider_url
│   │  ← worker.py    (LiveDataCollector, load_symbols, write_snapshot, DEFAULT_*)
│   │  ← canary.py    (validate_provider_url)
│   │  ← readiness.py (DEFAULT_STALE_AFTER_SECONDS, DEFAULT_STATUS)
│   │  ← tests/test_live_data.py  (LiveDataCollector, is_fresh, load_symbols,
│   │                                validate_provider_url)
│   │
│   └── BINANCE_SYMBOLS: 59 asset → USDT pair mappings
│       COINBASE_PRODUCTS: dynamic keys (not hardcoded) for secondary verification
│       _blockchain RPC: retry-once for transient 408/429/5xx errors
│       ProviderState.failures: reset to 0 on success (was only incremented)
│
├── worker.py ................................. ← 24/7 supervisor loop
│   │  → imports  src.live.collector
│   │  → mirrors  live_data/live_snapshot.json → blocks_deploy/{11}/
│   │  → writes   live_data/worker_status.json
│   │  → exposes  merge_with_previous, run_once
│   │  ← tests/test_live_data.py  (merge_with_previous)
│   │  ← deploy/live_worker.Dockerfile  (ENTRYPOINT)
│   │  ← deploy/docker-compose.live.yml
│   │
│   └── Key: partial outage → retains prior healthy observations
│       Stale observations marked "retained_from_previous_cycle"
│
├── canary.py ................................. ← provider connectivity verification
│   │  → imports  src.live.collector (validate_provider_url)
│   │  → tests    5 provider endpoints (fixture or live)
│   │  → writes   live_data/provider_canary_evidence.json
│   │  ← tests/test_live_data.py  (fixture_opener, run_provider_canary)
│   │
│   └── --fixture: no network  |  --live --confirm-live: real calls
│
└── readiness.py .............................. ← liveness/data-readiness evaluator
    │  → imports  src.live.collector (DEFAULT_STALE_AFTER_SECONDS, DEFAULT_STATUS)
    │  → reads    live_data/worker_status.json
    │  → exposes  evaluate_status
    │  ← deploy/docker-compose.yml  (healthcheck)
    │
    └── Separates: liveness (is worker running?) from
                   data_readiness (are observations fresh?)
```

---

## 5. CATALOG & BUILD PIPELINE

```
src/catalog/
│
├── build.py .................................. ★ CATALOG ENGINE
│   │  → reads    data/yield_data.csv
│   │  → reads    csv/source_snapshots/{59}.csv
│   │  → writes   data/asset_catalog.csv
│   │  → writes   csv/assets/{59}.csv
│   │  → writes   csv/quotes/{59}.csv
│   │  → mirrors  → blocks_deploy/{11}/asset_catalog.csv
│   │  → mirrors  → blocks_deploy/{11}/csv/quotes/
│   │  → exposes  QUOTE_FIELDS, QUOTE_SOURCE_FIELDS
│   │  ← audit.py  (QUOTE_FIELDS, QUOTE_SOURCE_FIELDS)
│   │  ← deploy/Restart-BlocksAgents.ps1
│   │
│   └── Derives: yield_momentum, mcap_to_tvl, risk_score, yield_premium
│       Ownership: .generated-by-build-asset-catalog marker required
│
├── audit.py .................................. ★ VALIDATION ENGINE
│   │  → imports  src.catalog.build (QUOTE_FIELDS, QUOTE_SOURCE_FIELDS)
│   │  → reads    data/yield_data.csv
│   │  → reads    data/DATA_DICTIONARY.md
│   │  → reads    data/asset_catalog.csv
│   │  → reads    csv/assets/{59}.csv
│   │  → reads    csv/quotes/{59}.csv
│   │  → checks   blocks_deploy/*/yield_data.csv (byte-identical)
│   │  → writes   docs/validate.md  (regenerated report)
│   │
│   └── Validates: 118 rows, 61 columns, aggregate formulas,
│       deployment copies, dictionary coverage, catalog consistency,
│       quote export integrity
│
└── organize.py ............................... ← source snapshot manager
    │  → reads/writes  csv/source_snapshots/*.csv
    │  → reads/writes  csv/reference/*.csv
    │  → reads/writes  csv/source_manifest.json
    │  ← tests/test_asset_sources.py  (MANIFEST, check)
    │
    └── --write: move table-*.csv → named files
        --check: verify SHA-256 integrity of all organized files
```

---

## 6. DEPLOYMENT PIPELINE

```
src/deployments/
│
├── fleet_gate.py ............................. ★ PREFLIGHT CHECK
│   │  → reads  blocks_deploy/{12}/*/agent-card.json
│   │  → reads  blocks_deploy/{12}/*/deployment-metadata.json
│   │  → reads  blocks_deploy/{12}/*/pyproject.toml
│   │  ← tests/test_packaging.py  (test_local_fleet_gate_is_no_spend)
│   │
│   └── Validates: 12 projects, required files, card/directory identity
│       Reports: external gates still required (blocks check, registration...)
│
├── sync.py ................................... ★ MIRROR SYNC ENGINE
│   │  → reads  38 SOURCE_FILES + 4 SOURCE_GLOBS patterns
│   │  → writes  blocks_deploy/{11}/* mirrors
│   │  → writes  deployment_mirror_manifest.json
│   │  ← tests/test_packaging.py  (test_generated_contracts_are_current)
│   │
│   └── Excludes: crypto_yield_a2a_orchestrator, credentials, live snapshots
│       --write: copy + manifest  |  --check: SHA-256 verify only
│
└── generate.py ............................... ← deployment generator
    │  → reads   blocks_agents/{agent}.json (cards)
    │  → generates  blocks_deploy/{agent}/handler.py wrappers
    │  → generates  blocks_deploy/{agent}/deployment-metadata.json
    │  ← tests/test_packaging.py  (test_generated_contracts_are_current)
    │
    └── --write: regenerate  |  --check: verify no drift
```

---

## 7. RELEASE PIPELINE

```
src/release/
│
├── gate.py ................................... ★ RELEASE VALIDATOR
│   │  → reads   release_record.json (optional)
│   │  → validates  20 REQUIRED_FIELDS
│   │  → checks   12 agent registry coverage
│   │  → checks   5 A2A specialist grants
│   │  → checks   8 private trigger case types
│   │  ← tests/test_packaging.py  (test_release_gate_rejects_blank_record_without_network)
│   │
│   └── --template: emit blank record  |  --record: validate populated record
│
├── trigger.py ................................ ★ GUARDED LIVE TRIGGER
│   │  → imports  blocks_network SDK (only when --live)
│   │  → requires BLOCKS_API_KEY env var
│   │  ← tests/test_packaging.py  (test_guarded_trigger_never_dispatches_by_default,
│   │                               test_guarded_trigger_requires_paid_acknowledgement,
│   │                               test_live_trigger_requires_key_before_sdk_import)
│   │
│   └── --dry-run: default (no SDK, no network)
│       --live --confirm-paid: dispatch one paid task
│
└── proof.py .................................. ← proof record generator
    └── generates release proof artifacts
```

---

## 8. GATEWAY (Node.js / TypeScript)

```
crypto_yield_matrix_node_gateway/
│
├── index.ts .................................. ★ GATEWAY ENTRY POINT
│   │  → imports  server.ts        (createGateway, parseClientKeys, etc.)
│   │  → imports  agents.ts        (AGENTS)
│   │  → imports  dotenv/config    (BLOCKS_API_KEY from .env)
│   │  → env      GATEWAY_PORT, GATEWAY_HOST, GATEWAY_CLIENT_KEYS,
│   │             GATEWAY_MAX_DAILY_TASKS, GATEWAY_TASK_COST_USD, ...
│   │  ← deploy/docker-compose.yml  (build context)
│   │  ← crypto_yield_matrix_node_gateway/Dockerfile
│   │
│   └── Creates gateway server → listen(port, host)
│       Shutdown: SIGINT/SIGTERM → drain → destroy
│
├── server.ts ................................. ★ HTTP API + BUDGET ENGINE
│   │  → imports  agents.ts        (AGENTS, getAgent)
│   │  → imports  llm.ts           (createLlmServiceFromEnv, LlmError)
│   │  → imports  @blocks-network/sdk  (TaskClient, textPart)
│   │  → uses     node:crypto      (randomUUID, timingSafeEqual)
│   │  → uses     node:fs          (readFileSync, writeFileSync, renameSync)
│   │  → uses     node:http        (createServer)
│   │  → uses     node:net         (isIP)
│   │
│   │  Endpoints:
│   │    GET  /health               → public liveness
│   │    GET  /ready                → readiness + budget snapshot
│   │    GET  /agents               → agent catalog
│   │    GET  /metrics              → authenticated JSON metrics
│   │    GET  /metrics/prometheus   → authenticated Prometheus text
│   │    POST /agents/:name/invoke  → authenticated paid task dispatch
│   │    POST /llm/chat             → authenticated LLM chat (Ollama/hosted)
│   │
│   │  Budget: per-UTC-day task/spend counters with atomic file persistence
│   │  Kill switch: file-based emergency stop
│   │  Rate limiting: per-client sliding window
│   │  Organization: optional tenant binding with per-org caps
│   │
│   │  ← index.ts            (createGateway, parseClientKeys, parseClientAgents,
│   │                          parseClientOrganizations, parseOrganizationLimits)
│   │  ← smoke.ts            (test imports)
│   │  ← resilience.ts       (test imports)
│   │
│   └── Exports: createGateway(), Gateway type, parseClientKeys(),
│       parseClientAgents(), parseClientOrganizations(),
│       parseOrganizationLimits(), isLoopbackHost()
│
├── agents.ts ................................. ← AGENT REGISTRY
│   │  → no imports (pure data)
│   │  ← server.ts  (AGENTS, getAgent)
│   │  ← index.ts   (AGENTS)
│   │  ← llm_smoke.ts
│   │
│   └── 12 agents with name, description, requestFields
│
├── llm.ts .................................... ← LLM TRANSPORT
│   │  → imports  node:dns/promises  (lookup)
│   │  ← server.ts  (createLlmServiceFromEnv, LlmError, LlmService)
│   │  ← llm_smoke.ts
│   │
│   └── Ollama (local, default) + Hosted (opt-in, HTTPS)
│       SSRF protection: blocks private/internal/metadata hosts
│
├── resilience.ts ............................. ← RESILIENCE TESTS
│   │  → imports  server.ts  (createGateway, parseClientKeys)
│   │  → imports  agents.ts  (AGENTS)
│   │  → tests    capacity, timeout, metrics, artifact caps
│   │
│   └── npm run resilience
│
├── smoke.ts .................................. ← SMOKE TESTS
│   │  → imports  server.ts  (createGateway, parseClientKeys)
│   │  → imports  agents.ts  (AGENTS)
│   │  → tests    auth, budget, validation
│   │
│   └── npm run smoke
│
├── llm_smoke.ts .............................. ← LLM SMOKE TESTS
│   │  → imports  llm.ts, agents.ts
│   │
│   └── npm run llm:smoke
│
├── package.json .............................. ← npm scripts + dependencies
│   │  deps: @blocks-network/sdk@1.0.11, dotenv
│   │  scripts: start, check, smoke, resilience, audit
│   │
├── tsconfig.json ............................. ← TypeScript config
├── Dockerfile ................................ ← gateway container image
└── docker-compose.yml ........................ ← local dev compose
```

---

## 9. TESTS

```
tests/
│
├── test_packaging.py ......................... ★ 10 INTEGRATION TESTS
│   │  → subprocess: src/deployments/fleet_gate.py
│   │  → subprocess: src/deployments/sync.py --check
│   │  → subprocess: src/deployments/generate.py --check
│   │  → subprocess: src/release/gate.py --template / --record
│   │  → subprocess: src/release/trigger.py (dry-run, live, confirm-paid)
│   │  → subprocess: python -m blocks_agents.handlers
│   │  → reads:      blocks_deploy/crypto_risk_analyst/deployment-metadata.json
│   │  → reads:      blocks_deploy/crypto_risk_analyst/agent-card.json
│   │  → imports:    src.release.trigger (live_trigger — in test method)
│   │  sys.path → project root (parents[1])
│   │
│   └── ROOT = parents[1] (project root) for subprocess cwd
│
├── test_asset_sources.py .................... ★ 6 ASSET SOURCE TESTS
│   │  → imports  blocks_agents.handlers.common (load_asset_snapshot,
│   │             snapshot_research, SOURCE_SNAPSHOT_DIR)
│   │  → imports  blocks_agents.handlers.crypto_risk_analyst (handler)
│   │  → imports  src.catalog.organize (MANIFEST, check)
│   │  → reads    csv/source_snapshots/*.csv
│   │  sys.path → project root (parents[1])
│   │
│   └── Tests: manifest completeness, snapshot loading, handler artifact,
│       uncovered assets, filename mismatch, one-row-per-file
│
├── test_live_data.py ......................... ★ 6 LIVE DATA TESTS
│   │  → imports  src.live.canary   (fixture_opener, run_provider_canary, default_specs)
│   │  → imports  src.live.collector (LiveDataCollector, is_fresh, load_symbols,
│   │                                  validate_provider_url)
│   │  → imports  src.live.worker   (merge_with_previous)
│   │  sys.path → project root (parents[1])
│   │
│   └── Tests: symbol loading, collector parsing, provider canary,
│       URL validation, merge retention, freshness rejection
│
└── run_live_tests.py ......................... ← RUNNER: calls all test_live_data tests
    │  → imports  test_live_data (5 test functions)
    │  sys.path → tests/ (sibling import)
    │
    └── 5/5 PASS when run from project root
```

---

## 10. WEB DASHBOARD

```
web/
│
├── index.html ................................. ★ DASHBOARD UI
│   │  → loads   matrix.js   (<script src="matrix.js">)
│   │  → loads   styles.css  (<link href="styles.css">)
│   │  → fetches data/yield_data.csv (via matrix.js)
│   │  ← deploy/docker-compose.yml  (volume mount)
│   │
│   └── Single-page crypto yield matrix dashboard
│
├── matrix.js .................................. ★ DASHBOARD LOGIC
│   │  → fetches  ../data/yield_data.csv
│   │  → renders  interactive yield matrix table
│   │  ← index.html  (loaded by)
│   │
│   └── Pure vanilla JS, no framework dependencies
│
└── styles.css ................................. ★ DASHBOARD STYLES
    ← index.html  (loaded by)
```

---

## 11. DEPLOY CONFIGURATION

```
deploy/
│
├── docker-compose.yml ........................ ★ PRODUCTION DEPLOY
│   │  → builds   crypto_yield_matrix_node_gateway/Dockerfile
│   │  → builds   live_worker.Dockerfile
│   │  → mounts   data/, live_data/, csv/, web/
│   │  → env      BLOCKS_API_KEY, GATEWAY_CLIENT_KEYS, etc.
│   │  → healthcheck: readiness.py evaluation
│   │  → ports    127.0.0.1:3000:3000  (private loopback)
│   │  → resources cpu=1.0, memory=512M (gateway) / cpu=0.5, memory=256M (worker)
│   │
│   └── Services: gateway + live_worker
│       Volumes: budget-state (persistent), live-snapshot (shared)
│
├── docker-compose.live.yml ................... ← LIVE WORKER STANDALONE
│   │  → builds   live_worker.Dockerfile
│   │  → env      LIVE_WORKER_CYCLE_SECONDS, LIVE_WORKER_TIMEOUT_SECONDS
│   │
│   └── Worker-only deployment for live data pipeline
│
├── live_worker.Dockerfile .................... ← WORKER IMAGE
│   │  → COPY     src/ → /app/src/
│   │  → COPY     data/yield_data.csv → /app/data/
│   │  → COPY     csv/source_snapshots/ → /app/csv/source_snapshots/
│   │  → ENTRYPOINT  python src/live/worker.py
│   │
│   └── Python 3.11+ with blocks-network SDK
│
├── Restart-BlocksAgents.ps1 .................. ← RESTART SCRIPT
│   │  → runs    python src/catalog/build.py
│   │  → runs    python src/deployments/sync.py --write
│   │  → runs    docker compose restart
│   │
│   └── Windows PowerShell deployment helper
│
└── monitoring/
    ├── prometheus.yml ........................ ← PROMETHEUS SCRAPE CONFIG
    │   → targets  gateway:3000  (/metrics/prometheus)
    │   → headers  X-Gateway-Organization (when tenant binding active)
    │
    └── alerting_rules.yml .................... ← 9 ALERT RULES
        └── gateway_down, budget_exhausted, capacity_saturated,
             high_5xx_rate, auth_abuse, task_timeout_spike,
             worker_liveness_failed, worker_stale_data
```

---

## 12. BLOCKS DEPLOYMENTS (12 native projects)

```
blocks_deploy/
│
├── crypto_risk_analyst/ ...................... ★ EXAMPLE: all 12 follow same pattern
│   │  → handler.py      (imports: blocks_agents/handlers/crypto_risk_analyst.handler)
│   │  → trigger.py      (imports: blocks_network SDK)
│   │  → agent-card.json (deployment-specific, synced identity from root card)
│   │  → pyproject.toml  (deps: blocks-network==1.0.11)
│   │  → deployment-metadata.json  (generated by generate.py)
│   │  → validate.md     (mirrored from root)
│   │  → yield_data.csv  (mirrored from root)
│   │  → asset_catalog.csv  (mirrored from build.py)
│   │  → csv/quotes/*.csv   (mirrored from build.py)
│   │  → csv/source_snapshots/*.csv  (mirrored from sync.py)
│   │  → handlers/common.py  (mirrored from sync.py)
│   │  → live_data/live_snapshot.json  (mirrored from worker.py)
│   │
│   │  ← fleet_gate.py  (validates all 12)
│   │  ← sync.py        (mirrors source files)
│   │  ← generate.py    (generates wrappers)
│   │  ← worker.py      (mirrors live snapshots)
│   │  ← build.py       (mirrors catalog + quotes)
│   │
│   └── Each deployment: self-contained Blocks-native project
│       Published with paid billing at $0.10/task
│
├── defi_liquidity_analyst/ .................. (identical structure)
├── yield_methodology_expert/ ................ (identical structure)
├── data_provenance_auditor/ ................. (identical structure)
├── tokenomics_sustainability_expert/ ........ (identical structure)
├── feature_engineering_expert/ .............. (identical structure)
├── matrix_research_insights_agent/ .......... (identical structure)
├── model_validation_guardian/ ............... (identical structure)
├── portfolio_scenario_expert/ ............... (identical structure)
├── quant_forecasting_expert/ ................ (identical structure)
├── crypto_research_communications_agent/ .... (identical structure)
│
└── crypto_yield_a2a_orchestrator/ ........... ★ A2A ORCHESTRATOR (excluded from mirrors)
    │  → agent-card.json  (orchestrator-specific)
    │  → handler.py       (custom: calls 5 specialists in parallel)
    │  → trigger.py       (imports: blocks_network SDK)
    │  → pyproject.toml   (deps: blocks-network==1.0.11)
    │
    └── NOT mirrored: has custom runtime, not data-consuming
        Calls: data_provenance_auditor, feature_engineering_expert,
               crypto_risk_analyst, defi_liquidity_analyst,
               tokenomics_sustainability_expert
```

---

## 13. DOCS

```
docs/
├── familytree.md .............................. ★ THIS FILE
├── audit.md ................................... ← production readiness audit (NO-GO gates)
├── audit2.md .................................. ← codebase health audit (all code fixes resolved)
├── setup.md ................................... ← setup instructions
├── a2a.md ..................................... ← A2A architecture documentation
├── beproof.md ................................. ← proof documentation
├── validate.md ................................ ← generated by audit.py (canonical CSV report)
├── incident_runbook.md ........................ ← incident response procedures
├── marketing.md ............................... ← marketing materials
└── packaging.md ............................... ← packaging documentation
```

---

## 14. DEPENDENCY FLOW SUMMARY (most important → least)

```
                        data/yield_data.csv
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
      common.py          build.py         collector.py
      (handlers)         (catalog)        (live data)
         │                  │                 │
    ┌────┼────┐        ┌────┼────┐       ┌────┼────┐
    ▼    ▼    ▼        ▼    ▼    ▼       ▼    ▼    ▼
  11    csv/  live    csv/  data/ csv/  live/ live/ blocks_
  handlers assets _data assets catalog quotes snap status deploy
    │                                       │
    │    ┌──────────────────────────────────┘
    │    │
    ▼    ▼
  blocks_deploy/{11 projects}/
  (mirrored by sync.py, build.py, worker.py)
         │
         ▼
  Blocks Network (published agents)
         │
         ▼
  crypto_yield_matrix_node_gateway/
  (index.ts → server.ts → agents.ts)
         │
         ▼
  Client applications (authenticated via GATEWAY_CLIENT_KEYS)
         │
         ▼
  web/index.html (dashboard, loads matrix.js → yield_data.csv)
```

---

## 15. CRITICAL PATH: What breaks if X changes

| File changed | Immediate impact |
|---|---|
| `data/yield_data.csv` | `audit.py` re-runs validation; `common.py` produces different results; `build.py` regenerates catalog; `collector.py` reloads symbols; all 11 handlers affected; all 11 deployment mirrors need update |
| `blocks_agents/handlers/common.py` | All 11 handlers break; `tests/test_asset_sources.py` breaks; 11 deployment mirrors go stale |
| `src/deployments/sync.py` SOURCE_FILES list | Mirror drift across all 11 deployments |
| `src/catalog/build.py` | `asset_catalog.csv`, `csv/assets/`, `csv/quotes/` go stale; downstream `common.py` enrichment changes |
| `crypto_yield_matrix_node_gateway/server.ts` | Gateway API stops working; all 12 agent invocations fail |
| `crypto_yield_matrix_node_gateway/index.ts` env defaults | Gateway won't start with wrong config |
| `src/live/collector.py` BINANCE_SYMBOLS | Live pricing for affected symbols drops |
| `src/release/gate.py` REQUIRED_FIELDS | Release validation changes |
| Any `blocks_agents/handlers/{agent}.py` | Only that agent's output changes |
| `web/matrix.js` | Dashboard breaks |
| `deploy/docker-compose.yml` | Container orchestration changes |

---

*Generated from codebase analysis — all imports, reads, writes, and mirrors traced.*
